name: Apply SAFE BPF patches (A16)

on:
  workflow_dispatch:

jobs:
  bpf:
    runs-on: ubuntu-22.04

    steps:
      # ============================
      # Checkout TARGET kernel
      # ============================
      - name: Checkout kernel
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ============================
      # Clone crDroid BPF source
      # ============================
      - name: Clone crDroid BPF source
        run: |
          git clone https://github.com/crdroidandroid/android_kernel_xiaomi_surya crdroid
          cd crdroid
          git checkout 16.0-bpf

      # ============================
      # Generate REALLY SAFE BPF patches
      # ============================
      - name: Collect SAFE BPF commits
        run: |
          mkdir -p patches
          cd crdroid

          git log --reverse --pretty=format:%H -- \
            kernel/bpf \
            kernel/bpf/syscall.c \
            kernel/bpf/verifier.c \
            kernel/sys.c \
            include/uapi/linux/bpf.h \
          | grep -Ei "bpf|ebpf|syscall|verifier" \
          | grep -Eiv "btf|debug|trace|selftest|perf|x86|arm64|arch" \
          > /tmp/bpf_list.txt

          wc -l /tmp/bpf_list.txt
          cat /tmp/bpf_list.txt

          while read c; do
            git format-patch -1 "$c" -o ../patches
          done < /tmp/bpf_list.txt

      # ============================
      # Apply patches safely
      # ============================
      - name: Apply patches
        run: |
          ls -lah patches || exit 0
          for p in patches/*.patch; do
            echo "Applying $p"
            git apply --check "$p" || continue
            git apply "$p"
          done

      # ============================
      # Commit & push
      # ============================
      - name: Commit
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git status
          git commit -am "kernel: backport SAFE eBPF support for A15/A16" || echo "Nothing to commit"
          git push
